.DS_Store
node_modules
/dist
public/wasm/*
public/res/*.json*
public/res/*.mp3
public/soundfont/
src/wasm/*

# Rewritten copies of source modules that the node tests import. Every test
# that makes one recreates it, so a stale copy is worse than no copy.
test/.tmp-*/

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
